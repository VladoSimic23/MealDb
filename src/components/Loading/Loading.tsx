import styles from "./style/loading.module.css";

const Loading = () => {
  return (
    <div className={styles.loading}>
      <div>
        <h1>Loading...</h1>
      </div>
    </div>
  );
};

export default Loading;
